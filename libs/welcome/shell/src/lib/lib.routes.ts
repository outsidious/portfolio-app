import { Route } from '@angular/router';
import { HelloPageComponent } from '@portfolio-app/welcome-feature-hello-page'

export const shellRoutes: Route[] = [{ path: '', component: HelloPageComponent }];
